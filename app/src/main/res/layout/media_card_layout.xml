<?xml version="1.0" encoding="utf-8"?>

<layout>
    <data>

        <variable
            name="click"
            type="android.view.View.OnClickListener" />

        <variable
            name="model"
            type="com.example.sameteam.homeScreen.bottomNavigation.taskModule.model.OverlapImageModel" />
    </data>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/rootLayout"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_marginTop="@dimen/scale_5dp"
        android:layout_marginEnd="@dimen/scale_8dp"
        android:onClick="@{(v)->click.onClick(v)}">

        <com.google.android.material.imageview.ShapeableImageView
            android:layout_width="@dimen/scale_60dp"
            android:layout_height="@dimen/scale_56dp"
            android:id="@+id/imageHolder"
            loadQbDialogPhoto="@{model.imageUrl}"
            app:shapeAppearanceOverlay="@style/roundedImageView"
            android:src="@drawable/image_placeholder"
            android:scaleType="centerCrop"/>

    </RelativeLayout>

</layout>

