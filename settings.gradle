

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven {
            url = uri("https://jitpack.io")
            credentials {
                username = settings.findProperty("jitpackUsername") ?: ""
                password = settings.findProperty("jitpackPassword") ?: ""
            }
        }
    }
}
rootProject.name = "YourAppName"
include(":app")

rootProject.name = "SameTeam"
include ':app'
